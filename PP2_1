//1. Declare variables
float y = 100;
float x = 0;

//2. Set display size
void setup() {
  size(200, 200);
  
}

//3. Draw shape. Increase increment
void draw() {
  background (204);
  ellipse(x, y, 10, 10);
  x+= 1;
  
}
